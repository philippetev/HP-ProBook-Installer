#!/bin/bash
RightNowIs=`date "+%Y-%m-%d %H-%M"`
BackupDir="$HOME/Desktop/Installer Backups/Backup $RightNowIs"
kextdir="$DSTVOLUME/System/Library/Extensions"
# removing the old kext.
if [ -e "$kextdir/IOath3kfrmwr.kext" ]
then
  if [ ! -e "$BackupDir" ]
  then
	mkdir -p "$BackupDir"
  fi
  cp -Rf "$kextdir/IOath3kfrmwr.kext" "$BackupDir"
  rm -rf "$kextdir/IOath3kfrmwr.kext"
fi