#!/bin/bash
if [ ! -r "$HOME/Desktop/Tools" ];
then
mkdir "$HOME/Desktop/Tools"
chown "$USER" "$HOME/Desktop/Tools"
fi
unzip "$DSTVOLUME/tmp/apps/Kext Wizard.app.zip" -d "$DSTVOLUME/tmp/apps/"
cp -Rf "$DSTVOLUME/tmp/apps/Kext Wizard.app" "$HOME/Desktop/Tools/"
chown -R "$USER" "$HOME/Desktop/Tools/Kext Wizard.app"
